# LinkedIn Session Data
# Browser session files for persistent login
# Keep essential auth files, remove cache directories

# Keep these files for session persistence:
Default/Cookies*
Default/Login*
Default/Preferences
Default/Local Storage/
Default/Sessions/
Local State
Default/Network Persistent State

# Cache directories (can be removed to save space):
# Default/Cache/
# Default/Code Cache/
# GrShaderCache/
# ShaderCache/
# Default/DawnGraphiteCache/
